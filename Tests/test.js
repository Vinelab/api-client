var angular = require('angular-mocks');
//it("should create a module ", function () {
//	angular.module("DataService", []);
//beforeEach(function() {
    /*module('DataService');
	module(function($provide) {
		$provide.service('dataFetcher', function() {
			this.url = mocha.createSpy('url');
			this.getData = mocha.createSpy('getData').andCallFake(function(uri, params){
				
			});
		});
		//Getting reference of the mocked service
		var mockUtilSvc;

		inject(function(util) {
			mockUtilSvc = util;
		});
	});*/
//});
//});

